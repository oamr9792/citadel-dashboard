{
  "name": "citadel-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx wrangler pages dev public --port 8788",
    "deploy": "npx wrangler pages deploy public --project-name citadel-dashboard"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
